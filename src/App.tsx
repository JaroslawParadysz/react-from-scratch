import React from "react";
import './styles.scss';

const App: React.FC = () => {

  return (
    <div className="app-wrapper">
      <h1>React 17 and TypeScript + WebPack and SASS</h1>
    </div>
  );
};

export default App;